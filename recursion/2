#include <bits/stdc++.h>
using namespace std;
void name(int b,int n)
{
    if(b>n)
    {
        return;
    }
    cout << "Nidhi" << endl;
    name(b+1,n);
}
int main()
{
    // int a;
    // cout<<"Enter the number of times->";
    // cin>>a;
    int a=5;
    name(1,a);
    return 0;
}
