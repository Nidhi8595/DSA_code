#include <bits/stdc++.h>
using namespace std;

void f(int i,int n)
{
    if (i<n)
    {
        return;
    }
    cout<<i<<endl;
    i=i-1;
    f(i,n);
}
int main()
{
    int a=5;
    // cout<<"Enter the number->";
    // cin>>a;
    f(a,1);
    return 0;
}
