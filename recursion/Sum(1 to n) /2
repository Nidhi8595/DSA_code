#include <iostream>
using namespace std;
// functional recursion
int sum(int n)
{
   if(n==0)
   {
    return 0;
   }
   return n+sum(n-1);
}
int main()
{
    int n;
    cout << "Enter the number upto where you want to find sum->" << endl;
    cin >> n;
    cout<<sum(n);
    return 0;
}
